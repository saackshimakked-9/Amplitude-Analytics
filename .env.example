# Copy to .env for local `vercel dev`. Never commit real keys.
# Amplitude → Settings → Projects → your project
AMPLITUDE_API_KEY=your_project_api_key
AMPLITUDE_SECRET_KEY=your_project_secret_key
# "us" (default) or "eu". Your project is US.
AMPLITUDE_REGION=us
